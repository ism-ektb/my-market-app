services:
  market-postgres:
    image: postgres:13.3
    container_name: market-upp
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=user
      - POSTGRES_DB=market
    ports:
      - '6432:5432'